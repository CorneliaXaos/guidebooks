--[[
  This file is used to easily procure default values.
]]

-- Guidebook Options
--------------------

local options = {
  dimensions = {
    index = 4,
    page = 5,
    height = 5
  },

  textures = {
    -- TODO fill in texture defaults!
  },

  max = {
    bookmarks = 5,
    shared = 15
  }
}

-- Return Defaults
------------------

return {
  options = options
}
